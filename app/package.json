{
  "name": "jenkins-sample-app",
  "version": "1.0.0",
  "description": "Sample Node.js application for Jenkins course",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node test.js",
    "build": "echo 'Building application...' && mkdir -p dist && cp server.js dist/ && cp package.json dist/",
    "lint": "echo 'Running linter...' && exit 0"
  },
  "keywords": ["jenkins", "nodejs", "ci-cd"],
  "author": "Jenkins Course",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2"
  }
}
