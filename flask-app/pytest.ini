[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    --verbose
    --strict-markers
    --tb=short
markers =
    integration: Integration tests
    unit: Unit tests
    slow: Slow tests
